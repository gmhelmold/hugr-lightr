services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_PASSWORD: s3-test
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 1s
      timeout: 1s
      retries: 10
  web:
    image: nginx:1.27-alpine
    ports: ["18080:80"]
    depends_on:
      db:
        condition: service_healthy
    profiles: ["default"]
