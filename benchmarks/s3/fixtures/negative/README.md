# Negative S3 Inputs

Runner unit tests cover malformed JSON, blank input, unknown fields/versions,
mixed evidence, duplicate tuples, missing fields, duplicate JSON keys,
non-integral/out-of-range JCS numbers, receipt mutation, and each pairing reason.
