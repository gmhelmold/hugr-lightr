# SI-00 execution receipt — native bootstrap delivered; package not accepted

**Date:** 2026-09-17. **Campaign:** #152. **WP:** #153. **Delivery:** draft PR #154.
**Authorization:** the owner explicitly requested SI-00 execution. No SI-01
release, protocol activation, user-store migration or merge follows from this receipt.
**Technical contract:** v2.2 at `e30811971f6a1c2a1e2fdb0a91dc8ccf9a06879d`.
**Integration base:** `19afb88d94db1d79cfe2a56ce7a99eab84f6abb3`.
**Latest executed bootstrap head:** `2f8a96dd2010a21fc5546984feb9713ccc88362c`.

## Published implementation, not an unapplied prototype

The PR contains the native workflow, `expected-tests.json` and nine Python
bootstrap scripts: `aggregate.py`, `baseline.py`, `evidence.py`, `fixtures.py`,
`inventory.py`, `native.py`, `probes.py`, `resource_probe.py`, `test_evidence.py`.
The canonical scripts were introduced at `e359bd20f653b869674986368ff608044d9b64e3`.
They are different from an earlier local prototype and were preserved when the
branch advanced during this session. No stale prototype patch replaced them.

`77062104640486ad7ce03e0e804630749d013106` added exact dirty-checkout diagnostics.
`7bc3ea3ef904145ec1b4b0a633cd1442584071a0` added Proposed ADR-0020, CONTRACTS.md
and EXPERIMENTS.md. `2f8a96dd2010a21fc5546984feb9713ccc88362c` repaired native
checkout setup before file materialization, retaining all provenance checks.

An earlier prototype upload was blocked by a tool-level safety check and was
not rerouted. That historical event is NOT the publication status of the
canonical e359 implementation. The previous version of this receipt and PR
summary overstated the remaining publication blocker; this receipt supersedes
those statements. The unreferenced prototype tree is not a delivered commit.

## Latest native matrix — verified artifacts and actual execution

[Run 35211106571](https://github.com/gmhelmold/hugr-lightr/actions/runs/35211106571)
executed merge checkout `2f3e7a8822e600b8e931961243cb3ea6837ed4b4`, with parents
`19afb88d94db1d79cfe2a56ce7a99eab84f6abb3` and
`2f8a96dd2010a21fc5546984feb9713ccc88362c`.
The source tree is `3a5d50a7b6539a893628de74e8720d5ec9b17f15` and input fingerprint
is `03ff525946eb62cd0bf3d71a810bc8e52d5684adf9ae311823879ab56a25056d`.
These are actual tested identities, not the later documentation commit.

| Native profile | Passing executions | Ignored executions | Formatting exit |
|---|---:|---:|---:|
| Linux x86_64 / ext4 | 120 | 1 | 1 |
| Linux aarch64 / ext4 | 120 | 1 | 1 |
| macOS x86_64 / APFS | 106 | 1 | 1 |
| macOS aarch64 / APFS | 106 | 1 | 1 |
| Windows x86_64 MSVC / NTFS | 106 | 0 | 1 |

Total: **558 passing executions and four historical ignores**, not 558 new
unique tests. The aggregate returned `SI00_NATIVE_MATRIX_VERIFIED` with
`runtime_qualified=false`. Formatting is deliberately reported as a failed
baseline gate, not silently turned green. The ignored Unix test is
`index::tests::r1_tests_gc::gc_reclaims_hard_killed_run_dir`; its baseline
allowlist does not exempt it or any required witness from future qualification.

The aggregate checked all five native artifacts against the intended checkout,
parents, input fingerprint, binary and artifact hashes, listed/executed tests
and the independently supplied smoke policy. Binary bytes are now retained.
The authoring session downloaded the matrix verdict and latest Windows archive,
verified their GitHub archive digests, and rehashed every Windows internal
artifact, including the two actual test executables. It did not independently
download all four other latest binary archives; those were checked by the CI
aggregate, not falsely described as separate offline inspection.

| Selected latest artifact | ID | Archive SHA-256 |
|---|---:|---|
| Native matrix verdict | 10492270466 | `f0a514b6e8ec5a32c92d2732f7cd1386526d7f52071d999f26ef79f9ad8ef972` |
| Windows native archive | 10492121434 | `380e750a2bfb19ccd71b693bfc38de38af06cea5ffcfdbb9e89bfd7d6ec8e813` |
| Latest CLI baseline | 10492525702 | `a78417020f5ff1474a2c8cc0e6c12ef878192559a3f82ed87a704b3a74c406e4` |

The GitHub artifacts expire on 2026-10-17. A durable delivery archive retains
these selected raw receipts/logs and the earlier comparison evidence. A checksum
alone does not replace retaining the file. No final-code performance budget is
registered or approved by these observations.

## Windows checkout defect — fixed and re-executed

At e359 and 770, the checkout changed `core.autocrlf` after actions/checkout.
770's diagnostics showed tracked changes before and after tests across the
checkout. The native validator correctly refused those inputs even though the
106 Windows tests passed. No dirty-input exception was introduced.

2f8 configures `core.autocrlf=false` BEFORE initial checkout on the disposable
hosted runner and requires clean tracked inputs before building. The new
Windows receipt records empty `tracked_status_before` and `tracked_status_after`,
both dirty flags false, 106 passing executions and `NATIVE_BASELINE_VERIFIED`.
The five-profile aggregate also passed. This is an observed bootstrap repair,
not a storage-protocol correctness claim or a change to a user's Git settings.

## Capability and resource probes

The latest native receipts exercised file synchronization on all profiles;
Unix directory synchronization and permission denial; native link construction
including file/directory and both dangling Windows link kinds. Both APFS
profiles exercised `clonefile`. ext4 and the tested NTFS profile reported native
clone NOT_EXERCISED, with copy fallback exercised separately. Windows does not
claim directory-entry power-loss durability. OS probes are not tests of future
Rust recovery or link-materialization code.

The evidence-tools job executes 18 canonical Python test methods, with multiple
negative subcases. They reject empty/filtered suites, identity drift, missing or
corrupt artifacts, wrong binaries/order, dirty inputs, duplicate JSON/test events,
missing benchmark evidence, late/stale budgets, invalid samples and numerical
regressions. An earlier local prototype passed 24 tests; those are NOT counted
as additional canonical tests or claimed present in the PR.

A real constrained-filesystem probe in run 35209414026 used a disposable
33,554,432-byte filesystem image, observed errno 28, removed only owned filler,
verified retained bytes and unmounted the image. The result explicitly states
`runtime_recovery_tested=false`. This establishes a usable OS test capability,
not completed C13 recovery qualification. The current evidence-tools job also
reports its resource step successful; no host disk was filled.

## Real CLI failures remain visible

Run 35209414026 at merge `eda7b188e390e9280077e01da0a1727599d1c52f` completed
F0-F4 sample sets with independent materialized-tree comparisons, then failed
F5's first snapshot with ENOENT. The archive is artifact 10491642452, SHA-256
`ad2ee19411da3328bece94a3884a53bcc6a16ec1deb13125c2ab24aee1eaab2c`.
F5 includes literal Unix backslash and link cases; a matching errno does not
prove the historical macOS incident has the same cause.

The LATEST run 35211106571 failed earlier, at **F1-09-new**, exit 1:
`lightr: No such file or directory (os error 2)`.
F0 completed 20 measured samples; F1 retained only six before failure. Do not
claim the latest candidate completed F0-F4, average away the failure, or present
partial F1 data as the registered 20-sample result. Its baseline status is
`BASELINE_FAILED`; the full workflow is not a successful qualification run.
This reproduces a current snapshot failure on Linux, not its exact root cause.

Formatting, this current ENOENT, the original incident and all storage defects
remain open. The new receipt collector is doing its job by preserving them.
No measurement constitutes an approved no-regression verdict. F6 still needs
the correct SI-03 history/reference implementation.

## Contracts and caller review actually delivered

Proposed ADR-0020 specifies bounded metadata framing and the intentional
compatibility changes relative to stat-index identity and Windows symlink
fallback. LMF1 and current RefRecord remain unchanged; no codec is implemented
here. The ADR is Proposed, not silently Accepted. Golden codec vectors and
full semantic/cfg caller ratification remain work to finish before acceptance.

CONTRACTS.md maps the selected seams, shared cache versus Store lock domains,
public callers and root families. In particular, the GC inventory includes
32-byte build Action Cache manifest roots as well as 72-byte LRR1 stdout/stderr
records. The canonical inventory covers 490 Rust files and 1,739 conservative
symbol occurrences; it is NOT a type-resolved call graph. EXPERIMENTS.md reserves
E01-E22 identifiers/oracles and sampling/incident methodology; names in that
document are not already implemented or passing remediation tests.

## Acceptance accounting — do not close #153 yet

| Obligation | Actual state |
|---|---|
| Native build/test/artifact capability | Delivered and matrix verified at the identity above; limitations explicit |
| Windows clean-checkout provenance | Fixed, rerun and verified |
| Evidence-tool controls and numeric comparator | Implemented and executed; not a fabricated performance budget |
| OS no-space test capability | Exercised on disposable image; future runtime recovery still untested |
| Fresh CLI baseline data | Collected with byte checks, but F1/F5 failures retained; no complete accepted performance baseline |
| Resource/root/caller map | Published conservative inventory plus reviewed groups; full typed/cfg ratification incomplete |
| Schema/support/compatibility decisions | Proposed ADR and bounded contract delivered; acceptance/golden vectors incomplete |
| SI-00 DoD and integration | Not accepted or merged; no G-FOUNDATION or SI-01 dispatch |

No production Rust file, dependency, real storage format or user data was changed.
The branch contains CI/scripts/docs only. No agent session, protocol activation,
main/integration merge, release, account-Project permission change or background
execution promise is made. Completing the Project remains a separate account
authorization issue, not the reason to block technical bootstrap work.

## Reproduction

Check out the recorded source head, not an unrelated current branch. Run:

```sh
python -m unittest discover -s scripts/si00 -p 'test_*.py' -v
python scripts/si00/native.py --profile windows-x86_64 --out fresh-native-results
python scripts/si00/inventory.py --output caller-inventory.json
```

The native command must run on the corresponding native host with Rust 1.96.0;
use a new output directory. For offline checking, use the canonical
`validate_native(receipt, root, expected, policy)` with independently pinned
checkout/input identities, not merely the receipt's own assertions. Preserve
raw archives and their hashes. Do not execute archived Windows binaries on a
foreign host or equate source-level models with runtime qualification.
