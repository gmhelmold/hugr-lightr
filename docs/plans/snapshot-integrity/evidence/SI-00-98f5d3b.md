# SI-00 execution — frozen vectors and real CLI defect witnesses

**Date:** 2026-09-17. **Campaign:** #152. **WP:** #153. **PR:** #154.
**State:** published bootstrap continuation, not package acceptance or integration.
**Plan:** v2.2 at `e30811971f6a1c2a1e2fdb0a91dc8ccf9a06879d`; all numbered
criteria and all five axioms remain unchanged.

## Source and evidence identities

The continuation was published without force-push:

- `01a8b38d4e650a124fdaf105fb7d5b6b9b5c845e`: frozen native-authored vectors,
  independent hash checks, UNDO/ADOPT precondition and no-regeneration CI.
- `98f5d3bc1146cbff964f8495a515e40c8e621c16`: bounded real-CLI diagnostics,
  positive controls, independent tree comparison and archived executable.

The tested merge checkout is `3952a49031884535baa46ba01ed5c52a4f09e696`,
parents `19afb88d94db1d79cfe2a56ce7a99eab84f6abb3` and `98f5d3bc1146cbff964f8495a515e40c8e621c16`,
source tree `ef908135d5f490e9c49fa67efc8a1b6ef9f7de71`, input fingerprint
`3aa755e2b4a5cf2cba6b644918b1e58873aa2e319d5a86b0937ef41d3aa88d1e`.

Native run: [35244264875](https://github.com/gmhelmold/hugr-lightr/actions/runs/35244264875).
Contract run: [35244264762](https://github.com/gmhelmold/hugr-lightr/actions/runs/35244264762).
The containing commit also preserves a locally exercised manual C diagnostic and
review documents. It must not be substituted for the executable identity above.
No production Rust, Cargo dependency, runtime format or user data changed.

## Frozen schema inputs: delivered, not still an unpublished patch

The ten native-authored golden frames are versioned in
`bootstrap/vectors/schema-vectors.json.gz`. Only storage compression changed;
the decoded JSON remains exactly 24,177 bytes with SHA-256
`8e0a4b7c52f99e12a8ecf91a4bdc706b547f5524d8e7464d06972e0b5b3221df`.
Normal CI consumes the immutable input without `--generate`. Its bounded loader
rejects changed, missing, oversized and over-expanding inputs. The original
native authorship remains run 35237459468 / source 01d71d7.

The locked native BLAKE3 helper and independent Python reference agree on these
bytes. The reference source and CC0 license are retained; it is test-only.
Eleven canonical checks passed in native-hash CI. Forty-two Python test methods
passed both locally and in CI, including 18 independent schema methods and four
diagnostic-support methods. Seven thousand and twenty strict frame prefixes are
rejected; those subcases are not counted as 7,020 separate runtime tests.
The old parser control fails twice when UNDO/ADOPT is allowed without a previous
reference; the fixed parser rejects them while preserving the ten valid frames.
This fixes the specification oracle, not a production Rust codec.

## Five-profile native execution and independent revalidation

| Profile | Passing executions | Historical ignored | Formatting exit |
|---|---:|---:|---:|
| Linux x86_64 | 120 | 1 | 1 |
| Linux aarch64 | 120 | 1 | 1 |
| macOS Intel | 106 | 1 | 1 |
| macOS Apple Silicon | 106 | 1 | 1 |
| Windows x86_64 MSVC | 106 | 0 | 1 |

All five native jobs and the aggregate passed the baseline evidence policy:
**558 executions, four explicit historical ignores, runtime_qualified=false**.
Formatting remains failed; this is not a green product or merge gate.

Unlike the earlier partial offline inspection, this round downloaded **all five
native archives**, checked their GitHub-provided archive digests, rehashed every
listed artifact and executable, parsed named test logs and ran `validate_native`
with independently supplied checkout/tree/parent/policy identities. The tracked
source fingerprint was also independently recomputed and matched. Foreign
executables were not run on this Linux host. The matching Linux CLI was run.

## Actual product observations

The untraced CLI baseline failed at **F3-08-new**, exit 1 / ENOENT. F0-F2 each
completed 20 measured samples; F3 retained five. F4/F5 were not reached in this
run. No partial result is relabeled as a complete performance baseline. The
previous F1 and F5 failures remain historical evidence, not erased by later runs.

The separate real-CLI diagnostic completed with valid positive controls:

| Case | Snapshot / hydrate --verify | Independent observation |
|---|---|---|
| Ordinary file | 0 / 0 | Exact tree match |
| Ordinary dangling symlink | 0 / 0 | Exact tree match |
| Literal backslash filename | 1 / not run | Trace shows wrong slash-normalized pathname opened with ENOENT |
| Literal backslash symlink target | 0 / 0 | Restored target changed from `absent\leaf` to `absent/leaf` |

All source content remained unchanged. The latter is a **silent fidelity defect
even with --verify**, not just an anticipated failure. The strace captures the
actual changed pathname and actual symlink creation. A file-byte verifier cannot
recover link information already corrupted in the manifest. Owner: SI-02/#148,
with affected materializer regression coverage in SI-03. Details and reproduction
are in `bootstrap/INCIDENT-DISPOSITION.md`.

## Controlled shared-temporary witness against the actual executable

CLI SHA-256: `67ac37728cce96d35e437f0146578657be9e4c810dfe358d4f7b9599c71e9e64`.
A locally compiled test-only interposer was applied only to a disposable Linux
subprocess. It freezes wall time used by the old temp naming scheme, preserves
monotonic time and real filesystem APIs, and synchronizes four completed opens.
The canonical `probe_temp_collision.py` runner was executed after its initial
prototype, with the same three required controls:

- Real clock / four workers: exit 0, one current reference.
- Fixed clock / one worker: exit 0, one current reference, completed barrier.
- Fixed clock / four workers: four distinct threads opened **one pathname and
  one inode**, then snapshot exited 1 with ENOENT and published no current ref.

No watchdog timed out and all source bytes remained unchanged. This establishes
missing exclusive temporary ownership under a controlled collision. It does
**not** attribute the historical macOS/F1/F3 incident, measure natural collision
frequency, qualify a future allocator, or claim E01/E02 remediation completion.
Owner: SI-01/#147. The propagation guard did prevent this failed snapshot from
publishing a ref. The manual diagnostic's success means a defect was measured;
it is deliberately not a product acceptance gate that requires the bug to remain.

C source SHA-256: `bbc3cae9f28bcc185cb9309a2b34efba08459bb276ec221f78a8f8aa49ebcc56`.
Local built interposer SHA-256: `4804f1998bb5b646923a8e68dc10c76d7b2c923a227e5c5c600e24303e214d95`.
Environment: Linux x86_64 / glibc 2.41, cc 14.2.0. Compile and raw subprocess
logs are retained in the durable evidence package. No privileged host mutation
or global LD_PRELOAD installation occurred.

## Caller review delta

The conservative census now has 491 Rust files / 1,739 textual occurrences.
The direct binding review distinguishes 73 named calls in 35 files: 70 relevant
Store/CAS/ref/root calls and three unrelated network/CRI atomic-write calls.
Tests, imports, local variables, image sidecars, raw32 build AC and LRR1 output
roots are distinguished. The old benchmark's direct namespace deletion is
recorded as an affected caller, not reused as trustworthy performance evidence.
`bootstrap/CALLER-REVIEW-ADDENDUM.md` identifies exact source locations/owners.

This completes the recorded direct-name disambiguation, not compiler-resolved
whole-program analysis or independent acceptance of the future cfg conversion.
The review is by the author/coordinator, not an invented second agent.

## Durable artifact identities

| Artifact | ID | Archive SHA-256 |
|---|---:|---|
| Native verdict | 10506603071 | `d0d2563614515a265b32f787e836ae0f4515b50b378b2d39e7124effb4545386` |
| Linux x86_64 | 10506767342 | `816c3deb18fa47fdee4f36a1e8a05613a48f473b6be0814dd47c13a1121826b7` |
| Linux aarch64 | 10506388298 | `943515ac589660332f1e41eca0154666579d2a7a9097bf2b8f484fb36b569b1d` |
| macOS Intel | 10507330092 | `25556172a15fef29c81f209bf305d9864cc36bf0e771e61c7f868e4f2869175b` |
| macOS Apple Silicon | 10506812402 | `c1b4b48d99f1de2dec763504ff08621d7f3b120b1df9888280e1abbf5a17fbda` |
| Windows MSVC | 10507016956 | `73db7981916d507d3bd7355dbcb3aa05408eafb0373d1aedee2b2464432b39c8` |
| CLI / traces / binary | 10506463555 | `d7eae6847486930d16163d12140f5cadebea305303b2b3084343fcffff26820c` |
| Contract / source / hash helper | 10506049865 | `3a335ac1681785e5ceff1d1e5dd99b1dfabebf87800662e7630d638c44b653b2` |

Raw files, not just their checksums, are retained in the deliverable. GitHub's
copies expire 2026-10-17. Historical receipts remain versioned and explicitly
refer to their own code/runs; this receipt supersedes their *current* status.

## Acceptance accounting and next integration boundary

Frozen vectors, their publication/no-regeneration gate, the operation-precondition
fix, real incident diagnostics and direct binding review are now delivered.
The remaining package boundary is **recorded final compatibility/seam ratification
and reviewed integration** under DOD00.1-DOD00.4. There is no missing blanket user
authorization and no current GitHub write blocker. The ADR is still Proposed;
this receipt does not silently accept it or treat author self-review as external
review. Full baseline runtime correctness/performance still needs the planned
remediation; zero-errno failures cannot be waived to obtain a green merge.

SI-00/#153 remains open. SI-01 is not dispatched, and neither G-FOUNDATION nor
G-ACTIVATION is established. No main or integration merge, release, real-store
migration, agent session, account-permission or billing change was performed.
The plan's 123 criteria are not reduced to substitute evidence counts for DoD.
