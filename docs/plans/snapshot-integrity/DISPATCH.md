# Snapshot integrity — publication and execution state

**Date:** 2026-09-16  
**Specification:** [SNAPSHOT-INTEGRITY-REMEDIATION.md](../SNAPSHOT-INTEGRITY-REMEDIATION.md), version 2.2.  
**Campaign:** #152. **Bootstrap:** SI-00 #153.  
**Integration:** `fix/snapshot-integrity` / PR #146 (draft).  
**Revision base:** `fda407e11de7516a812c321ed29bd1798fc48edc` (v2.1).  
**Unchanged audited Rust baseline:** `e4a53417f6fe7da8c4f44908af9525536443d0fa`.

## Authorized change

The owner approved the integrated-review refinements within the existing planning scope. This commit changes planning Markdown only. It does not execute SI-00, accept ADRs, alter Rust/tests/workflows, enable the new storage protocol, migrate data, launch agents, merge, release or change permissions/billing.

V2.2 preserves six WPs, 30 axiom sections, all 123 criterion IDs, 14 global invariants and E01–E22. Thirty existing criterion texts are extended, not deleted. Integrated-review A01–A05 now have selected contracts and causal acceptance obligations: actual namespace/lock domains, native symlink/parent path semantics, additive inert foundation followed by G-ACTIVATION, independent pending pre-scan with no current, and rejection of empty/echo-only performance evidence. A06 native tracking is recorded separately below.

**Specification Git blob:** `5317d313c7edee03f83742173888191c9a885745`. **Specification SHA-256:** `e9c44406190b154e3a7f7fec24889806249b42a24f1ac666bccb9609e4d7f23b`. The containing commit supplies the document version; there is no self-referential SHA requirement. WP issues are synchronized to that commit and exact criteria in the publication step; live issue bodies and the campaign record are the authoritative receipt of successful synchronization.

## Work state — all NOT STARTED

| Package | Issue | Prerequisites / meaning of completion |
|---|---|---|
| SI-00 | #153 | Later implementation authorization; accepted domains/seams/activation plan, native capability and valid baseline-evidence bootstrap |
| SI-01 | #147 | SI-00; G-FOUNDATION certifies inert primitives and records production_protocol_enabled=false |
| SI-02 | #148 | Pure portion after SI-00; actual primitive integration after G-FOUNDATION; no production activation |
| SI-03 | #149 | Integrated SI-01/02 and accepted composite ADR; ordered slices culminate in coordinated G-ACTIVATION on disposable stores |
| SI-04 | #150 | Actual activated SI-03 candidate; causal proof of public paths and all required experiments |
| SI-05 | #151 | Frozen SI-04 candidate; native/resource qualification and real comparison against registered budgets |

G-ACTIVATION is a source-integration gate, not production rollout, SI-05 qualification or owner authorization to migrate data. Earlier models or a green primitive suite do not establish it. One WP can have multiple reviewed PRs, but coupled acceptance remains one obligation. Worker PRs target `fix/snapshot-integrity`; issue closure requires its own DoD/evidence/integrated SHA, not a closing keyword on an intermediate PR.

## Integrated-review disposition

| Item | Planning response / actual state |
|---|---|
| A01 | Shared cache uses its actual resource-domain EX sections; foreign Store leases cannot authorize its cleanup. C02/C13 and E18/E22. Not implemented. |
| A02 | Native anchored component resolution or explicit rejection; no lexical collapse across unresolved symlinks. C12/E22. Not implemented. |
| A03 | Inert SI-01/02 plus coordinated SI-03 G-ACTIVATION; existing old-path bugs are not declared fixed. Not executed. |
| A04 | Strict independent pending namespace pre-scan, including no-current create/untag states. C06/E08/E11. Not implemented. |
| A05 | SI-00 evidence gate and SI-05 numeric comparison specified. Historical run 35158370442's zero-artifact/echo-only aggregator remains uncorrected by docs. |
| A06 | Native hierarchy/dependency/Project configuration remains incomplete through this connection; no successful mutation is claimed. |

## Native tracking limitation — do not relabel text as a relationship

This session re-read #152's native sub-issue endpoint: empty. The integrated review separately checked all WP blocked_by endpoints: empty. Current action discovery exposes file/issue writes but no native sub-issue/dependency/Projects mutations. Installed-provider discovery returned no additional connected executor for those operations. No authenticated GitHub CLI is available in the authoring container. No such mutation request or new HTTP 403 occurred, and the limitation does not diagnose the owner's permissions. A Project was not configured by this work; existing Projects v2 across the account were not inspected, so do not create a duplicate blindly.

Desired parent: #152 → #153, #147, #148, #149, #150, #151. Desired blocking edges (dependent → prerequisite): 147→153; 148→153; 148→147; 149→147; 149→148; 150→149; 151→150. The 148→147 edge governs production integration/DoD, not the permitted isolated pure portion. Retain these same issues. Desired Project stages remain Backlog, Ready, In progress, Review, Integrated, Done, with blockers and no invented milestone/deadline. Native completion requires actual supported writes and read-back; it is not a runtime correctness gate.

## Evidence and resume

Read the live plan, campaign and WP bodies, reconcile their commit/criteria identities and verify actual head before dispatch. No execution assignment is made here. Structural/diff checks of this amendment are planning evidence only; original CI/ENOENT and source defects remain unresolved. Existing CI may trigger from a documentation push; that is not new implementation. The separate benchmark-evidence workflow is not condemned from the defective CI aggregator; each needs its own artifacts.

Preserve the pre-v2 Copilot assignment receipt (403, no session); it was not retried. No recurring monitor or background dispatch exists. Later execution must use real resource-domain locks, exact native path identities, isolated primitive fixtures, the accepted activation boundary and an independent pending scan. Qualification remains SI-05 and main merge remains a separate owner decision.
