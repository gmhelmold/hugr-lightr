# critest skip-list — R1 narrowed; one ginkgo regex per line.
# Format: <regex>  # reason → R2
# These are joined by | and passed to critest as --ginkgo.skip.
# Conservative: skip whole Describe blocks by keyword to avoid
# partial-suite fragility. Add conquered items to tests/GREENLIST instead.
#
# R1 GRADED FAMILIES (removed from skip-list in WP-G R1):
#   - Streaming (Exec/Attach/PortForward): WP-C/D implement SPDY+WS; graded R1.
#     (streaming lines OWNED by the SPDY agent — do not add/remove here)
#   - Networking (Network/CNI/PodNetwork/HostNetwork/DNS/PortMapping/Portforward):
#       WP-B/D implement CNI bridge+host-local+portmap + netns lifecycle; graded R1.
#   - Container log (CRI log format): WP-A implements log tee + CRI format; graded R1.
#   - Image default-command (starting container): WP-A implements command from
#       image config; graded R1.
# These families now surface as CANDIDATE items on the first green R1 linux run;
# lead appends confirmed passes to tests/GREENLIST.
#
# CONFORMANCE FIXES APPLIED (2026-06-13, non-streaming):
#   - empty command default entrypoint: FakeBackend substitutes pause-loop when
#       container command+args are empty (fixes "failed to start container: invalid
#       argument: empty command").
#   - resolv.conf synthesis: sandbox DnsConfig → write per-sandbox resolv.conf;
#       bind-mount over /etc/resolv.conf in CLONE_NEWNS pre_exec on Linux
#       (fixes "DNS config" spec: cat /etc/resolv.conf assertions).
#   - hostname UTS namespace: sandbox hostname → unshare(CLONE_NEWUTS)+sethostname
#       in pre_exec on Linux (fixes "set hostname" spec).
#   - portmap hostPort=0 omit: host_port==0 entries filtered from runtimeConfig
#       portMappings before passing to CNI portmap plugin (fixes "Invalid host
#       port number: 0").
#   - critest_scoped: removed critest invocation from cargo test (was duplicating
#       ci/critest-gate.sh and aborting workspace on failure).

# RuntimeConfig / UpdateRuntimeConfig — UNIMPLEMENTED (literal code per
# r1-kubelet-smoke.md law; any other code = kubelet startup fails). R2 N/A.
[Rr]untime[Cc]onfig  # reason → R2
[Uu]pdate[Rr]untime[Cc]onfig  # reason → R2
[Rr]untime [Cc]onfig  # reason → R2

# Container stats beyond basic (metrics descriptors, WritableLayer stats) —
# fake backend returns zeroed values; descriptor-level assertions fail.
[Ww]ritable[Ll]ayer  # reason → R2
[Mm]etrics [Dd]escriptor  # reason → R2
[Cc]ontainer[Ff][Ss][Ss]tats  # reason → R2
[Ii]mage[Ff][Ss][Ss]tats  # reason → R2

# Checkpoint/restore — UNIMPLEMENTED; no checkpoint engine in scope.
[Cc]heckpoint  # reason → R2
[Rr]estore  # reason → R2

# Events / GetContainerEvents — evented PLEG alpha off in kubelet 1.33;
# DESCOPED to R2 per build-spec-r1 §1.
[Ee]vent  # reason → R2
[Cc]ontainer[Ee]vents  # reason → R2
[Pp][Ll][Ee][Gg]  # reason → R2

# UpdateContainerResources — UNIMPLEMENTED; no cgroup resource update path.
[Uu]pdate[Cc]ontainer[Rr]esources  # reason → R2
[Rr]esource [Uu]pdate  # reason → R2

# ReopenContainerLog — tolerated per kubelet matrix (log spam only); not
# blocking R1 but the describe block still fails on assertions.
[Rr]eopen  # reason → R2
[Rr]eopen[Cc]ontainer[Ll]og  # reason → R2

# Security Context: namespace/capability/no-new-privs subfamilies still need
# separate runtime proofs. SeccompProfilePath is now wired through seam v1.2,
# but stays in this aggregate skip until a real Linux critest run witnesses the
# exact family. Remove only after that gate is green.
Security Context  # reason → R2

# AppArmor — needs a real LSM/AppArmor enforcement engine (load + verify + reject
# unloadable profiles); the fake has none, so the "should error on unloadable
# profile" specs fail (expected error, got nil). Same class as Security Context.
# Owner-authorized R2 (2026-06-19).
AppArmor  # reason → R2

# Container Mount Readonly — needs privileged mount support (tmpfs/bind-mount
# syscalls require CAP_SYS_ADMIN; fake backend has no mount namespace). R2.
should support non-recursive readonly mounts  # reason → R2

# Container Mount Propagation — needs privileged mount support (bind-mount
# propagation requires real mount namespace + CAP_SYS_ADMIN). R2.
Mount Propagation  # reason → R2

# Container OOM — needs cgroup memory limits (cgroup v1/v2 memory.limit_in_bytes;
# fake backend runs plain host processes with no cgroup isolation). R2.
OOMKilled  # reason → R2
should output OOMKilled reason  # reason → R2

# Multiple Containers — pod shared-namespace semantics (PID/IPC/net sharing
# requires real namespace sharing; fake does not implement). R2.
Multiple Containers  # reason → R2

# PodSandbox sysctls — needs real Linux kernel sysctl namespace. R2.
should support.*sysctls  # reason → R2
runtime should support sysctls  # reason → R2

# Image Manager — fake images are synthetic CAS records; critest expects real
# OCI image semantics. All 7 items failed; none passed in R0. R2.
public image with tag should be pulled and removed  # reason → R2
public image without tag should be pulled and removed  # reason → R2
public image with digest should be pulled and removed  # reason → R2
image status should support all kinds of references  # reason → R2
image status get image fields should not have Uid  # reason → R2
listImage should get exactly 3 image in the result list  # reason → R2
listImage should get exactly 3 repoTags  # reason → R2
public image should timeout if requested  # reason → R2

# Networking with a real workload (port-mapping + portforward) — RUNTIME-TIER.
# critest's networking container uses the webserver IMAGE's ENTRYPOINT (no
# Command/Args override; pkg/validate/networking.go createWebServerContainer),
# i.e. the HTTP server is a real image binary. The fake has no image layer and
# runs HOST binaries, so there is no server to listen on the CNI IP -> the test
# curl gets connection refused. Not fake-conquerable; the lightr backend
# conquers it (materialize image from CAS + run its entrypoint inside the sandbox
# netns). CNI portmap DNAT + the dial-in-netns wiring are already correct.
# Owner-directed conquest attempted; proven runtime-tier 2026-06-20.
port mapping  # reason → R2 (runtime-tier: real image HTTP server)
portforward  # reason → R2 (runtime-tier: real image HTTP server)
