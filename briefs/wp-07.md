WP-07 | GPU-DEVICE | --privileged/--read-only/--shm-size/--pids-limit/--cpus/--memory native/vz/ns
Branch: wp/gpu-device
Target (FROZEN — C-05): Add `GpuParser`, `DeviceParser`, `CapParser` to `lightr-run/src/limits/` (C-05 frozen — agent reads limits.rs frozen interface `ResourceLimits` + `Ulimit`). Agent implements engine-specific appliers: native (`check_native_support` honest Err for unenforceable caps, `RLIMIT_AS` for memory on Linux, `Err` for cpu/pids on native); ns (`cgroup_v2::apply` writes `memory.max`/`cpu.max`/`pids.max` + `memory.swap.max=0` per #90 fix); vz (`shim FFI` `memorySize`/`cpuCount`). Agent NEVER edits `limits.rs` frozen interface (`ResourceLimits` fields, `check_native_support` signature, `apply_native`/`apply_cgroup` signatures) — only creates new parser/validation helpers in `limits/` directory or `crates/lightr-engine/src/engine/limits.rs`.
Files FIRST: crates/lightr-run/src/limits.rs (frozen — read only), crates/lightr-engine/src/engine/limits.rs (frozen — engine limits mirroring native), crates/lightr-cli/src/cli/cmd/subcommands.rs (frozen: SystemCmd/EngineCmd — agent may add `--privileged` as run flag ONLY via C-01 freeze), crates/lightr-cli/src/cli/dispatch.rs (frozen: dispatch match — agent adds branch ONLY if C-01 allows).
Contract: .techlead/contracts/gpu-device-caps.md
Disjoint from WP-01/02/03/04 (build/run/network/volume): limits lives in `lightr-run/src/limits/` + `lightr-engine/src/engine/limits.rs`; CLI flags added via C-01 frozen enum; no overlap with build/network/volume modules.
Acceptance (mutation-probe required):
  A. `lightr run --engine native --memory 64m -- /bin/sh -c 'echo ok'` exits 137 (SIGKILL/OOM) — CI proves `memory.max` binds with `swap.max=0` (#90).
  B. `lightr run --engine native --cpus 0.2 -- /bin/sh -c 'while true; do :; done'` runs slower than uncapped (5.05× — #90 proves throttle binds).
  C. `lightr run --engine native --privileged -- /bin/echo ok` exits 2 with stderr `not a sandbox` (honest — F-203 pattern).
  D. Mutation: remove `check_native_support` body in limits.rs → `cargo test -p lightr-run` fails (tests expect honest error for cpu/pids/memory off-Linux) → restore → measure before/after (PASS → RED → PASS).
Invariants: native honest Err for unenforceable caps (cpu share, pids limit, memory off-Linux); ns cgroup v2 for enforceable; vz FFI for VM-level; `--privileged` honest error exit 2 on native; <400 LOC/file (limits file may grow — split to limits/gpu_tests.rs + limits/device_tests.rs + limits/caps_tests.rs if >400); fmt/clippy clean; no dead-code windows; parallel-safe (injected `ResourceLimits` param, not global env).
Stop: "WP-07 SEAL: branch wp/gpu-device @ <sha> | clippy -p lightr-run -D: pass | tests: N passed, j2 | mutation: red/restore measured (before: PASS / after: PASS) | ambiguities: none | LOC max: 387"
