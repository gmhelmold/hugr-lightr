WP-01 | BU-01 | BuildKit feature parity
Branch: wp/buildkit
Target (FROZEN — contract C-04-NEW): Add flags --cache-from, --secret, --ssh to `lightr docker build` (cli/cmd/subcommands.rs: ComposeCmd/PlanCmd/OciCmd enums frozen — agent adds to BuildCmd only, not other enums) and wire to native `lightr build` handler (`crates/lightr-cli/src/handlers/build.rs` — frozen interface: `run(context, dockerfile, tag, engine, build_args, target)`).
Files to read FIRST (before any edit):
  crates/lightr-cli/src/cli/cmd/subcommands.rs  (frozen: BuildCmd enum)
  crates/lightr-cli/src/cli/dispatch.rs         (frozen: dispatch match arm)
  crates/lightr-cli/src/handlers/build.rs        (frozen: run() signature)
  crates/lightr-cli/src/handlers/docker/mod.rs  (frozen: translate_build — build_arg/target forwarding)
  crates/lightr-build/src/exec/buildkit/         (new: buildkit-extensions contract C-04-NEW)
Contract anchor: .techlead/contracts/buildkit-extensions.md
Disjoint: WP-01 owns ONLY buildkit-extensions files; WP-02 (COMPOSE+) owns ONLY compose files; C-04-NEW freeze `build/exec.rs` interface means WP-01 NEVER edits build/exec.rs (only creates new modules in buildkit/ directory that build/exec consumes via frozen interface).
Acceptance (real test, not prose):
  A. `lightr docker build --build-arg FOO=bar --target stage -t @t/app .` exits 0 with stderr mentioning build.
  B. `lightr build --cache-from @t/app --secret id=foo,src=/tmp/secret .` exits 0 or 1 (honest). If secret/ssh blocked: exit 2, stderr "not yet supported".
  C. Mutation-probe: break `run()` in build.rs (comment out body) → `cargo test -p lightr-acceptance --test acceptance_r3 a23_build_hydrate` goes RED → restore → measure (before: PASS / after: PASS). Report measured times.
Invariants: no `#[allow]`, <400 LOC/file (split tests: buildkit_tests.rs if needed), `pub use` in lib.rs for new module, fmt/clippy clean, no dead-code on windows (`cfg(unix)` gate + fallback stub).
Stop: "WP-01 SEAL: branch wp/buildkit @ <sha> | clippy -p lightr-build -D: pass | tests: 47 passed, j2 | mutation: red/restore measured | ambiguities: none | LOC max: 387"
Agent does NOT merge. Lead merges after cold-check (mutation-probe + diff review + full gate).
