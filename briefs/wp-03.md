WP-03 | RUN-01 | Full networking (DNS/VPN/network-create/IPAM)
Branch: wp/network
Target (FROZEN — contract C-03): Add DnsResolver, VpnManager, BridgeManager (create/connect/rm/inspect/ls), IpamAllocator to `lightr-run/src/network/` WITHOUT changing shared `NetworkId`/`VolumeId` types (`lightr_core` frozen — agent reads, never edits). Frozen surfaces: `network/mod.rs` (public interface), `run/types.rs` (port/network models — no edit unless contract updated by lead). Agent creates `dns.rs`, `vpn.rs`, `bridge.rs`, `ipam.rs` modules; re-exports in `network/lib.rs`.
Files FIRST: crates/lightr-run/src/network/mod.rs (frozen), crates/lightr-run/src/network/registry.rs (frozen — members/subnet), crates/lightr-core/src/network.rs (frozen — NetworkId), crates/lightr-cli/src/cli/cmd/subcommands.rs (frozen: NetworkCmd enum — agent may ADD variants ONLY if C-01 frozen and lead approves), crates/lightr-cli/src/handlers/network.rs (frozen interface: create/ls/rm/inspect — agent extends, not replaces).
Contract: .techlead/contracts/runtime-network-volume.md
Disjoint from WP-04 (volume): network owns network/; volume owns volume/. Both use `lightr_core::VolumeId` / `NetworkId` — frozen in core, independent.
Acceptance:
  A. `lightr network create --driver bridge mynet` exits 0; `ls` shows `mynet` with `bridge` driver.
  B. `lightr network rm mynet` exits 0 (if empty); exits 1 with "has active endpoints" if members > 0.
  C. Mutation-probe: remove `create()` body from network/registry.rs → `cargo test -p lightr-acceptance --test acceptance_r2` goes RED (tests depend on network registry) → restore → measure.
  D. No `--p` port binding changes (F-304 `-p` already shipped independently); DNS/VPN must not break existing `-p` path.
Invariants: <400 LOC/file; new `pub mod` in `network/lib.rs`; no process-global env mutation; `cfg(unix)` gates + `cfg(windows)` stub; fmt/clippy clean; no `#[allow]`.
Stop card exact format.
